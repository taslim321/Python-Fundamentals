#tuple is immutable
tup = ( 2, 4, 5, 6, 7)
print(type(tup))
# tup[2] = 10 # not allowed
tp=() # khali tuple
tt=(1) # like integer
print(type(tt))
ti =("string") #like string so we have to add ,
#slicing as like as list